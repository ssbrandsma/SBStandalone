# Station Artwork

This directory contains `radio.png`, the compact generic fallback used by the Standalone Radio Now Playing screen. It is installed with the applet and loaded locally with SqueezePlay's `Surface:loadImage()` API.

Built-in presets use this generic image to keep the Applet Installer ZIP small. Radio Browser stations can replace it with a supported favicon cached on the Radio after playback starts.
